# Session Log: 2026-09-24 — llama.cpp v0.5.0 Changelog Pass (repo-goal mapping)

Analytical pass over the upstream `v0.5.0` release notes (notes cover `v0.4.1`
→ `v0.5.0`; pinned engine `b11149` sits 3 commits past the tag). No rig runs —
each item mapped to the repo Pareto axes (`ctx × TPS × agentic × coding`) with
an explicit verdict: **ACT** (concrete follow-up), **NOTE** (background win),
**N/A** (out of scope + why).

## Goal

Find every `v0.5.0` change that can help or improve the autotuning goal, and
record the resulting follow-ups so they are not lost.

## Hardware

Desk research only; relevance judged for the operator rig (discrete 8 GB-class
NVIDIA, Windows 11, 32 GB-class host RAM, CUDA backend).

## Setup

- Source: `gh release view v0.5.0 --repo ggml-org/llama.cpp --json body`
  (258 lines, pulled 2026-09-24).
- Cross-checked against the pinned `b11149` binaries (`--help`) and existing
  repo coverage (`speculative-decoding-formats.md`,
  `capability-extraction-harness.md`, `advanced-inference-optimizations.md`,
  `thinking-models-claw-harness.md`, `docs/models/deepseek-v4.1-flash-dspark.md`).

## Commands (reproducible, repo-relative)

```powershell
Set-Location llama.cpp-releases\upstream\b11149\build-cuda\bin
.\llama-server.exe --help | Select-String "graph|LLAMA_|dedup-cache|fit"
.\llama-server.exe --help | Select-String "\(env: LLAMA_ARG_(TEMP|TOP_|MIN_|REPEAT|PRESENCE|FREQUENCY)"
```

## Findings

### P0 — do first (engine not yet model-validated)

- **No model has loaded on `b11149` yet.** The upgrade smoke covered
  `--version` + CUDA init only. Before any Trial: one embedded-MTP load smoke
  (Nemotron-3.5 path, extended by upstream #29018 — see below) plus one
  agentic-quick on a thinking model (behavior change in #28869).

### TPS axis

- **ACT — CUDA graphs for MTP drafting (upstream #28549).** Graphs are compiled
  into the CUDA backend and automatic (no flag; disable is env-only per
  `advanced-inference-optimizations.md`). MTP is the repo's main TPS lever, so
  this is a free uplift candidate: re-bench one embedded-MTP target on `b11149`
  vs its recorded `b10867` number. Zero harness change.
- **NOTE — MoE correctness/perf:** top-k MoE always fires (#28432), CUB argsort
  in-place fix (#28389). Core workloads here are MoE (Ornith, Qwen) — background
  win, no action.
- **NOTE — Flash Attention:** Gemma-4 Ampere tune (#29152), sparse FA for
  qwen4/dsv4 prefill (#28770, #29298), FA smem swizzle refactor (#28536).
  Relevant only when those archs are trialed.
- **NOTE — sampler probe shrink (#29285):** faster server start; marginal Trial
  iteration gain.
- **N/A — CUDA conv2d implicit GEMM (#29135):** vision path; text rig unaffected.
- **N/A — Metal/SYCL/HIP/Hexagon/OpenCL/Vulkan work:** wrong backends for this rig.

### Speculative decoding (TPS lever)

- **ACT (conditional) — Gemma-4 DSpark draft backbone (#29226).** `draft-dspark`
  itself is old (Qwen3 dense drafts since ~`b10375`, speculators-format since
  #26275); the `v0.5.0` delta is Gemma-4-backbone drafts. Repo verdicts on DSpark
  stay negative on 8 GB-class MoE — revisit only if a Gemma-4 target is trialed,
  and touch `speculative-decoding-formats.md` then, not now.
- **ACT (minor) — draft dedup via `dedup-cache-models` (#27934).** Stops duplicate
  HF draft downloads through server presets. Use in validation flows that pull
  draft repos (disk hygiene).
- **N/A — DFlash for HunyuanOCR (#28890):** OCR arch.

### Ctx axis

- **ACT — auto-fit max ctx with unified KV changed (#28849).** The repo sizes
  manually (`tools/fit-params` + `--fit-print`); the behavior under those tools
  moved. Re-run one `fit-print` sizing probe on `b11149` before trusting old
  numbers for unified-KV contexts.
- **ACT — scheduler-reserve graph diagnostics (#26625).** Engine now reports graph
  inputs/input tensors at reserve time: new first-class OOM-triage signal. Cite
  it in OOM session logs going forward.
- **NOTE — SWA pattern in model saver (#29042):** better conversion fidelity for
  SWA archs; the KV estimator already charges `min(ctx, window)`.
- **NOTE — GGUF data-section alignment (#28993):** mmap behavior; background.

### Agentic + coding axes

- **ACT — qwen3-coder forces `\n</think>` on reasoning-budget end (#28869).**
  Termination behavior for thinking models changed at the budget edge, and the
  repo's `max_tokens` floor (4096) plus `thinking-models-claw-harness.md`
  remasure policy exist exactly for this failure class. Re-run agentic-quick on
  one thinking model on `b11149` to confirm no `length_stop` regression before
  trusting old scores.
- **NOTE — parser fixes** (Ling 3.0 #28682, DeepSeek V3.2/V4 #29008, Muse
  Glimmer #29242, Gemma-4 tool grammar #29115) **+ JSON Schema/PEG/jinja fixes**
  (#28518, #29127, #29161, #29244): tool-call reliability wins; matter only when
  those families are trialed.
- **N/A — `input_image` function outputs (#22575):** text-only claw harness.
- **N/A — router/lifecycle fixes (#29217, #29212, #28938), multi-address `--host`
  (#28690), httplib 0.57.1, token-count crash fix (#29309):** single-localhost
  single-load harness; robustness background only.

### Provenance / harness (cross-cutting, highest value-per-cost)

- **ACT — capture `llama-bench --version` into Trial evidence (#28971).** Engine
  tag is currently hand-recorded; the binary now self-reports
  (`0.5.0-dev build 11149`). Wire it at server start in `llama_runner.py`
  (exact lines for the implementer to confirm against `arg.cpp`-first rule) so
  every `results.db` row carries its engine build.
- **ACT (minor) — sampler env vars exist, verified locally (#27380):**
  `LLAMA_ARG_TEMPERATURE/TOP_K/TOP_P/MIN_P/REPEAT_PENALTY/PRESENCE_PENALTY/FREQUENCY_PENALTY`
  plus the `LLAMA_ARG_SPEC_*` family all appear in `b11149 --help`. No harness
  change (CLI already forwards the 7 sampler keys); one-line note in
  `llamacpp-flags-audit.md` env coverage, useful for `model-up` launches outside
  harness plumbing.
- **NOTE — startup/model-source logging (#29125):** better launch triage.
- **NOTE — Ubuntu-CUDA release builds (#28186):** future WSL option.
- **N/A — RPC protocol v7:** no RPC backends in the harness.
- **N/A — UI changes, HRM-Text/Mimir (#27625), Qwen4Exp hyper-connections
  (#28901), muse-glimmer `--fuse-qkv` (#29203), ufakzeka tokenizer (#29033),
  gguf-py Q8_1 fix, NMSE save-load test, causal-compare-logits recipe:** future
  archs or conversion QA; discovery watch only. MiMo-V2.6 conversion (#29257)
  matters only for native MiMo-V2.6 GGUFs (repo alias is a Qwen-based distill).

## Errors (self-correction)

- The `b11149` upgrade log phrased `draft-dspark` as "new in v0.5.0". Imprecise:
  the flag predates this release (repo measured it on `b10375`-era builds); the
  `v0.5.0` delta is the **Gemma-4 draft backbone** (#29226). This log supersedes
  that phrasing — the upgrade log itself stays unedited per session contract.
- First read of the fetched notes file failed (binary-sniffed: UTF-8 with long
  lines); fell back to `Get-Content -Encoding utf8` chunk reads.

## Decisions

- Accepted follow-ups, priority order: **P0** model-load smoke on `b11149`
  (Nemotron-MTP + thinking-model agentic-quick) → **P1** MTP TPS re-bench,
  `fit-print` re-size, `--version` provenance wiring → **P2** flags-audit env
  note, DSpark doc touch only with a Gemma-4 Trial.
- This pass changes no tracked files except itself: model-card TBDs (e.g.
  `deepseek-v4.1-flash-dspark.md` "verify in `b10867 --help`" items, now
  answerable from the `b11149 --help` probes above) are left for their owning
  passes per `docs/models/` contract.
- Open question: measured size of the #28549 MTP-draft graph uplift on 8 GB-class
  CUDA — answered by the P1 re-bench, not estimated here.
